import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrl: './app.component.scss'
})
export class AppComponent {

  // costomerList: string[] = ['costomer1', 'costomer2', 'costomer3' , 'costomer4'];

  
  // pushCustomer() {
  //   this.costomerList.push('costomer' + (this.costomerList.length + 1));
  // }

  // unshiftCustomer() {
  //   this.costomerList.unshift('costomer' + (this.costomerList.length + 1));
  // }

  // removeCustomer(index: number) {
  //   this.costomerList.splice(index, 1);
  // }
  // title = 'my-app';
  // appMinLabel = 'myAppMinLabel';
  // appMaxLabel = 'myAppMaxLabel';
  // squaureHeight = 100;
  // squaureWidth = 250;

  // activate: boolean = false;

  // appCounter = 20;

  // testClick() {
  //   console.log('test EventBinding');
  // }

  // testNumberChange(value: number) {
  //   console.log('test NumberChange form app action bar: ', value);
  // }

  // doAppMinChange(value: number) {
  //   console.log('test minChange event: ', value);
  // }

  // doAppMaxChange(value: number) {
  //   console.log('test maxChange event: ', value);
  // }

  // value = 0; // ราคาหลังคำนวณบุฟเฟ่ต์มา4จ่าย3

  // calBuffet(value: string) { // ฟังก์ชันคำนวณราคาบุฟเฟ่ต์มา4จ่าย3
  //   const price = Number(value); // แปลงค่าที่รับมาเป็นตัวเลข
  //   this.value = (price * 3)/4; // คำนวณราคาบุฟเฟ่ต์มา4จ่าย3
  // }


  // ninjaName = 'Naruto'; //ค่แปรเก็บชื่อนินจา

  // ninjaConsole () { //ฟังก์ชันแสดงค่าชื่อนินจาในคอนโซล
  // console.log('console ninjaName:', this.ninjaName); //แสดงค่าชื่อนินจา
  // } }

  // changnNinjaName (name: string) { //ฟังก์ชันเปลี่ยนค่าชื่อนินจา
  // this.ninjaName = name;  //เปลี่ยนค่าชื่อนินจา
  // }
}
